class Cannon {
  //propriedades da classe
  constructor(x, y, w, h, angle) {
    this.x = x;
    this.y = y;
    this.w = w;
    this.h = h;
    this.angle = angle;

    //images
  }

  //funções da classe
  show() {
    //base do canhao
    //topo do canhao
  }
}
